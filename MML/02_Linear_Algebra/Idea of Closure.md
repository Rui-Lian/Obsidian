
#Closure
- Input set: Objects (Vectors), 
- Rules and operation, 
- outcome sets
What is the outcome sets that come from the input set by the rules and operations. In linear algebra, what are the sets of vectors that comes from a set of input vectors by adding and scaling. 

The resulting set is a [[Vector Space]]

The concept of [[Vector Space]] and its properties underly much of machine learning. 

